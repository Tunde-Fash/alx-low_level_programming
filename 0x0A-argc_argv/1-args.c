#include "main.h"

/**
 * count_arguments - Counts the number of arguments passed to the program.
 * @argc: The argument count.
 *
 * Return: The number of arguments.
 */
int count_arguments(int argc)
{
	return (argc - 1);
}
